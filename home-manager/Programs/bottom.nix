{
  programs.bottom = {
    enable = true;

    # This the catppuccin mocha setup.
    # TODO: raise an issue in stylix for bottom module.
    settings = {
      styles = {
        theme = "gruvbox";
      };
    };
  };
}
